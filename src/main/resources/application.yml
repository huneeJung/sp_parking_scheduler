spring:
  application:
    name: sp_parking_api
  datasource:
    url: jdbc:mysql://localhost:3306/parking
    username: ENC(UZKxgIDuA26/DA2/J6Btf2eEa8RuaOaS)
    password: ENC(G3qSSN0DpypzQtFhfSiuUWv4c/4C01rT)
    #    url: jdbc:mysql://localhost:3306/parking
    #    username: parking_api
    #    password: parking_api!
    hikari:
      maximum-pool-size: 10     # ???? connection ? ???? Pool ? ????? ?? Connection ??
      minimum-idle: 10          #  DBCP ?? ???? ?? ??(Idle) ???? ?, App ? ??? ? ?? ??? ? ?? ???? ?? ??? ??
      idle-timeout: 600000      # ?? ???? ???? ?? (Idle ???) Connect ? Pool ?? ???? ?? ??, 10?
      connection-timeout: 30000 # Application ?  DBCP ?? Connection ? ??? ? ??? ? ?? ?? ??, 30?
      max-lifetime: 1800000
  ## Hibernate
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL5InnoDBDialect

## Tomcat
server:
  port: 8081
  tomcat:
    threads:
      max: 200                # ??? ? ?? thread ? ? ??
      min-spare: 10           # ?? ??? ???? (idle) Thread ? ??
    max-connections: 8192     # ?? ??? Connection ? ? ??
    connection-timeout: 20000 # ?????? ?? ???? ??? ? ?? ??, 20?
    accept-count: 100         # ?? Connection ? ? ?? , max-connections ? ?? ???, Queue ? ?? Connection ? ??
    keep-alive-timeout: 100   # ??? ?????? ??? ??? ???? ???? HTTP ??? ???? ?? ??, 100?

parking:
  openapi:
    url: http://openapi.seoul.go.kr:8088
    key: 71546e4250636a7335355344596566
    type: json
    service: GetParkInfo

## ???
jasypt:
  encryptor:
    key: ${jasypt_key}

## Logging
logging:
  level:
    root: info
  pattern:
    console: "[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n"
